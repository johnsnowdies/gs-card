{
	"folders":
	[
		{
			"path": "."
		}
	],
     "build_systems":
    [
        {
            "name": "My Make (fix+clean+build)",
            "shell_cmd": "make fix && make clean && make build",
            "working_dir": "${project_path:${folder}}",
            "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$"
        }
    ]
}
